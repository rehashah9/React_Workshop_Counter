//import React from 'react'
import ReactDOM from 'react-dom/client'
//import App from "./App.jsx"
import Homepage from "./Pages/Homepage"

//here in the html code we can have maxinum 1 parent tag inside it we can have as
//many child tags as we want
ReactDOM.createRoot(document.getElementById('root')).render(<Homepage />) //rendering the content returned by App